{
  "$schema": "https://cursor.com/schemas/environment.schema.json",
  "build": {
    "context": ".",
    "dockerfile": "Dockerfile"
  },
  "user": "ubuntu",
  "install": "./.cursor/install.sh",
  "start": "",
  "ports": [
    {
      "name": "NextJS",
      "port": 3000
    },
    {
      "name": "FastAPI",
      "port": 8000
    }
  ],
  "terminals": [
    {
      "name": "NextJS",
      "command": "pnpm --filter web-nextjs dev",
      "description": "Starts the Next.js development server on port 3000."
    },
    {
      "name": "FastAPI",
      "command": "pnpm fastapi-dev",
      "description": "Starts the FastAPI backend server with auto-reload on port 8000."
    },
    {
      "name": "ExpoWeb",
      "command": "pnpm my-expo-app start --web",
      "description": "Starts the Expo development server for the web."
    }
  ]
}